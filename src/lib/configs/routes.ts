export const ApiRoutes = {
  devices: '/devices',
  session: './session',
}

export const AppRoutes = {
  home: '/',
  login: '/login',
  objects: '/objects',
  accounts: '/accounts',
  users: '/users',
  drivers: '/drivers',
  notifications: '/notifications',
  tasks: '/tasks',
}
